import streamlit as st
import google.generativeai as genai
import pandas as pd
import os

# --- 1. НАСТРОЙКИ ПРИЛОЖЕНИЯ ---
st.set_page_config(page_title="МОДО", layout="centered")

# Получение ключа из Secrets
API_KEY = st.secrets.get("GOOGLE_API_KEY", "")
TEACHER_PASSWORD = "admin" 
DATA_FILE = "results.csv"

if not API_KEY:
    st.error("Критическая ошибка: Вставьте API Ключ в настройки Streamlit (Secrets)!")
    st.stop()

# НАСТРОЙКА ИИ (Исправленный метод для обхода ошибки 404)
try:
    genai.configure(api_key=API_KEY)
    # Используем самое стабильное имя модели
    model = genai.GenerativeModel('gemini-1.5-flash')
except Exception as e:
    st.error(f"Ошибка инициализации ИИ: {e}")

# --- 2. ЛОГИКА ХРАНЕНИЯ ДАННЫХ ---
def save_result(fio, subject, answers):
    new_data = pd.DataFrame([{"ФИО": fio, "Предмет": subject, "Ответы": answers}])
    if os.path.exists(DATA_FILE):
        df = pd.read_csv(DATA_FILE)
        df = pd.concat([df, new_data], ignore_index=True)
    else:
        df = new_data
    df.to_csv(DATA_FILE, index=False)

# --- 3. ИНТЕРФЕЙС ---
role = st.sidebar.radio("Выберите роль:", ["Ученик", "Учитель"])

if role == "Ученик":
    st.title("📱 Тестирование МОДО")
    fio = st.text_input("👤 Ваше ФИО")
    
    subject = st.selectbox("📚 Выберите предмет:", 
                          ["Биология", "Математическая грамотность", "География", "Химия", "English", "Грамотность чтения"])

    with st.form("quiz_form"):
        st.write(f"### Тест по предмету: {subject}")
        
        if subject == "Биология":
            q1 = st.radio("1. Основной орган выделительной системы (фильтр):", ["Почки", "Печень", "Нефрон", "Мочевой пузырь", "Мочеточник"])
            q2 = st.radio("2. Структурно-функциональная единица почки:", ["Лоханка", "Пирамида", "Извитой каналец", "Нефрон"])
            q3 = st.radio("3. Процесс фильтрации крови происходит в:", ["Мочеточниках", "Лоханке", "Капиллярном клубочке"])
            q4 = st.radio("4. В состав первичной мочи НЕ входит:", ["Глюкоза", "Белок", "Вода", "Витамины"])
            q5 = st.radio("5. Обратное всасывание происходит в:", ["Канальцах нефрона", "Капсуле", "Лоханке"])
            q6 = st.radio("6. Объем вторичной мочи в сутки:", ["180 л", "150 л", "1,5 л", "10 л"])
            q7 = st.radio("7. Гормон регуляции мочи:", ["Вазопрессин", "Инсулин", "Адреналин"])
            q8 = st.radio("8. Центр мочеиспускания в:", ["Головном мозге", "Спинном мозге", "Мозжечке"])
            q9 = st.radio("9. Воспаление лоханок:", ["Цистит", "Пиелонефрит", "Энурез"])
            q10 = st.radio("10. Продукт обмена белков:", ["Мочевина", "Гликоген", "Желчь"])
            answers = f"Био: 1-{q1}, 2-{q2}, 3-{q3}, 4-{q4}, 5-{q5}, 6-{q6}, 7-{q7}, 8-{q8}, 9-{q9}, 10-{q10}"

        elif subject == "Математическая грамотность":
            q1 = st.radio("1. Время выезда из Алматы (ущелье):", ["8:15", "8:25", "8:35", "8:40"])
            q2 = st.radio("2. Корень уравнения 2x + 5 = 15:", ["2", "5", "10", "7"])
            q3 = st.radio("3. Периметр квадрата (S=64):", ["32 см", "16 см", "24 см"])
            q4 = st.radio("4. Значение (120-40):4+5:", ["25", "15", "45"])
            answers = f"Мат: 1-{q1}, 2-{q2}, 3-{q3}, 4-{q4}"

        elif subject == "География":
            q1 = st.radio("1. Переезд из РК в США на ПМЖ:", ["Эмиграция", "Иммиграция", "Урбанизация"])
            q2 = st.radio("2. Фактор алюминиевой пром-сти:", ["Сырьевой", "Энергетический", "Водный"])
            q3 = st.radio("3. Житель Астаны переехал в Хьюстон. Кто он для США?", ["Иммигрант", "Эмигрант", "Репатриант"])
            answers = f"Гео: 1-{q1}, 2-{q2}, 3-{q3}"

        elif subject == "Химия":
            q1 = st.radio("1. Причина щелочности озера:", ["Гидролиз соли", "Испарение", "Осадки"])
            q2 = st.radio("2. Тип связи в H2O:", ["Ковалентная полярная", "Ионная", "Металлическая"])
            q3 = st.radio("3. Реакция нейтрализации:", ["Кислота+Основание", "Металл+Вода"])
            answers = f"Хим: 1-{q1}, 2-{q2}, 3-{q3}"

        elif subject == "English":
            q1 = st.radio("1. She ... to school every day.", ["go", "goes", "going"])
            q2 = st.radio("2. We ... football yesterday.", ["play", "played", "playing"])
            q3 = st.radio("3. Give me ... apple.", ["a", "an", "the"])
            answers = f"Eng: 1-{q1}, 2-{q2}, 3-{q3}"

        elif subject == "Грамотность чтения":
            st.info("Текст 'Роза' (стр. 76 сборника)")
            q1 = st.radio("1. Чего нет в тексте?", ["Окраска решетки", "Сорная трава", "Крапива-лекарство"])
            q2 = st.radio("2. 'Роза точно плакала' это:", ["Роса", "Грусть", "Жара"])
            answers = f"Чтение: 1-{q1}, 2-{q2}"

        if st.form_submit_button("✅ Отправить результаты"):
            if fio:
                save_result(fio, subject, answers)
                st.success(f"Ответы {fio} приняты!")
                st.balloons()
            else:
                st.error("Введите имя!")

elif role == "Учитель":
    st.title("🔐 Кабинет учителя")
    if st.text_input("Пароль", type="password") == TEACHER_PASSWORD:
        if os.path.exists(DATA_FILE):
            df = pd.read_csv(DATA_FILE)
            st.dataframe(df)
            
            if st.button("🤖 Проверить через ИИ"):
                with st.spinner('Сверяю ответы...'):
                    try:
                        # Эталон ответов из PDF (стр. 76)
                        keys = "Био: 1-Почки, 2-Нефрон, 3-Клубочек, 4-Белок, 5-Канальцы. Чтение В1: 1-Е, 2-А, 3-C."
                        prompt = f"Ты эксперт МОДО. Сверь ответы учеников с эталоном: {keys}. Данные: {df.to_string()}. Сделай отчет."
                        response = model.generate_content(prompt)
                        st.markdown(response.text)
                    except Exception as e:
                        st.error(f"Ошибка ИИ: {e}. Попробуйте обновить страницу.")
        else:
            st.info("Результатов пока нет.")

