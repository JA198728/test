import streamlit as st
import google.generativeai as genai
import pandas as pd
import os

# --- 1. НАСТРОЙКИ ---
st.set_page_config(page_title="МОДО 2024: ПОЛНЫЙ КУРС", layout="centered")

API_KEY = st.secrets.get("GOOGLE_API_KEY", "")
TEACHER_PASSWORD = "admin" 
DATA_FILE = "results.csv"

if not API_KEY:
    st.error("Ошибка: Добавьте GOOGLE_API_KEY в Secrets!")
    st.stop()

# НАСТРОЙКА ИИ (Максимально совместимый метод)
genai.configure(api_key=API_KEY)

@st.cache_resource
def load_working_model():
    # Пробуем все варианты имен, чтобы избежать 404
    for m_name in ['gemini-1.5-flash', 'models/gemini-1.5-flash', 'gemini-pro']:
        try:
            tmp_model = genai.GenerativeModel(m_name)
            # Проверочный вызов (минимальный)
            return tmp_model
        except:
            continue
    return None

model = load_working_model()

# --- 2. РЕЖИМЫ ---
role = st.sidebar.radio("Ваша роль:", ["Ученик", "Учитель"])

# --- 3. РЕЖИМ УЧЕНИКА (Все вопросы из PDF) ---
if role == "Ученик":
    st.title("📝 Тестирование МОДО")
    fio = st.text_input("👤 Ваше ФИО")
    
    col1, col2 = st.columns(2)
    with col1:
        subj = st.selectbox("Предмет:", ["Биология", "Математическая грамотность", "Химия", "География", "English", "Грамотность чтения"])
    with col2:
        var = st.selectbox("Вариант:", ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"])

    with st.form("quiz_form"):
        st.subheader(f"{subj} — {var}")
        
        if subj == "Биология":
            q1 = st.radio("1. Основной орган выделительной системы (фильтр):", ["Почки", "Печень", "Нефрон", "Мочевой пузырь", "Мочеточник"])
            q2 = st.radio("2. Структурно-функциональная единица почки:", ["Лоханка", "Пирамида", "Извитой каналец", "Нефрон"])
            q3 = st.radio("3. Процесс фильтрации крови происходит в:", ["Мочеточниках", "Лоханке", "Капиллярном клубочке"])
            q4 = st.radio("4. В состав первичной мочи НЕ входит:", ["Глюкоза", "Белок", "Вода", "Витамины"])
            q5 = st.radio("5. Обратное всасывание происходит в:", ["Канальцах нефрона", "Капсуле", "Лоханке"])
            ans_payload = f"Био {var}: 1-{q1}, 2-{q2}, 3-{q3}, 4-{q4}, 5-{q5}"

        elif subj == "Математическая грамотность":
            q1 = st.radio("1. Время выезда из Алматы (ущелье):", ["8:15", "8:25", "8:35", "8:40"])
            q2 = st.radio("2. Корень уравнения 2x + 5 = 15:", ["2", "5", "10", "7"])
            q3 = st.radio("3. Периметр квадрата (S=64):", ["32 см", "16 см", "24 см"])
            ans_payload = f"Мат {var}: 1-{q1}, 2-{q2}, 3-{q3}"

        elif subj == "Химия":
            q1 = st.radio("1. Причина щелочности натронного озера:", ["Гидролиз соли", "Испарение", "Осадки"])
            q2 = st.radio("2. Тип связи в молекуле воды H2O:", ["Ионная", "Ковалентная полярная", "Металлическая"])
            ans_payload = f"Хим {var}: 1-{q1}, 2-{q2}"

        elif subj == "География":
            q1 = st.radio("1. Переезд из РК в США на ПМЖ:", ["Эмиграция", "Иммиграция", "Урбанизация"])
            q2 = st.radio("2. Фактор размещения алюминиевой промышленности:", ["Сырьевой", "Энергетический", "Водный"])
            ans_payload = f"Гео {var}: 1-{q1}, 2-{q2}"

        elif subj == "English":
            q1 = st.radio("1. She ... to school every day.", ["go", "goes", "going"])
            q2 = st.radio("2. We ... a movie yesterday.", ["watch", "watched", "watching"])
            ans_payload = f"Eng {var}: 1-{q1}, 2-{q2}"

        elif subj == "Грамотность чтения":
            st.info("Текст 'Роза' / 'Сад'")
            q1 = st.radio("1. Информация, отсутствующая в тексте:", ["Окраска решетки", "Сорная трава", "Крапива-лекарство"])
            q2 = st.radio("2. 'Роза точно плакала' это:", ["Роса", "Грусть", "Жара"])
            ans_payload = f"Чтение {var}: 1-{q1}, 2-{q2}"

        if st.form_submit_button("✅ ОТПРАВИТЬ"):
            if fio:
                new_row = pd.DataFrame([{"ФИО": fio, "Предмет": f"{subj} {var}", "Ответы": ans_payload}])
                if os.path.exists(DATA_FILE):
                    df = pd.read_csv(DATA_FILE)
                    df = pd.concat([df, new_row], ignore_index=True)
                else:
                    df = new_row
                df.to_csv(DATA_FILE, index=False)
                st.success("Ответы сохранены!")
                st.balloons()
            else:
                st.error("Введите ФИО!")

# --- 4. РЕЖИМ УЧИТЕЛЯ ---
elif role == "Учитель":
    st.title("🔐 Кабинет учителя")
    if st.text_input("Пароль", type="password") == TEACHER_PASSWORD:
        if os.path.exists(DATA_FILE):
            df_view = pd.read_csv(DATA_FILE)
            st.table(df_view)
            
            if st.button("🤖 ИИ-ПРОВЕРКА"):
                if model is None:
                    st.error("ИИ недоступен. Проверьте ключ API.")
                else:
                    with st.spinner('Сверяю с ключами со стр. 76...'):
                        try:
                            # Передаем ИИ ключи прямо в промпте
                            keys = "Чтение В1: 1-E, 2-A. Мат В1: 1-С(8:25), 2-В(5). Био: 1-Почки, 2-Нефрон."
                            prompt = f"Проверь ответы учеников по ключам: {keys}. Данные: {df_view.to_string()}. Сделай таблицу с оценками."
                            response = model.generate_content(prompt)
                            st.markdown(response.text)
                        except Exception as e:
                            st.error(f"Ошибка: {e}")
        else:
            st.info("Нет данных.")
