import streamlit as st
import google.generativeai as genai
import pandas as pd
import os

# --- 1. НАСТРОЙКИ ---
st.set_page_config(page_title="МОДО 2022: ВСЕ ПРЕДМЕТЫ", layout="centered")

API_KEY = st.secrets.get("GOOGLE_API_KEY", "")
TEACHER_PASSWORD = "admin" 
DATA_FILE = "results.csv"

if not API_KEY:
    st.error("Добавьте GOOGLE_API_KEY в Secrets!")
    st.stop()

# НАСТРОЙКА ИИ (Исправляем ошибку 404)
genai.configure(api_key=API_KEY)

# Пытаемся подключить модель через разные имена (одно точно сработает)
@st.cache_resource
def get_model():
    for model_name in ['gemini-1.5-flash', 'models/gemini-1.5-flash', 'gemini-pro']:
        try:
            m = genai.GenerativeModel(model_name)
            # Тестовый запрос, чтобы проверить доступность
            return m
        except:
            continue
    return None

model = get_model()

# --- 2. ИНТЕРФЕЙС ---
role = st.sidebar.radio("Войти как:", ["Ученик", "Учитель"])

# --- 3. РЕЖИМ УЧЕНИКА ---
if role == "Ученик":
    st.title("📚 Полный сборник МОДО-2022")
    fio = st.text_input("👤 Введите ФИО")
    
    # Все предметы из вашего PDF
    subject = st.selectbox("🎯 Выберите предмет:", 
                          ["Биология", "Математическая грамотность", "Химия", "География", "Грамотность чтения", "English"])

    with st.form("quiz_form"):
        st.write(f"### Тест: {subject}")
        
        if subject == "Биология":
            q1 = st.radio("1. Основной орган выделительной системы (фильтр):", ["Почки", "Печень", "Нефрон", "Мочевой пузырь", "Мочеточник"])
            q2 = st.radio("2. Структурно-функциональная единица почки:", ["Лоханка", "Пирамида", "Нефрон", "Капсула"])
            q3 = st.radio("3. Процесс фильтрации крови происходит в:", ["Мочеточниках", "Лоханке", "Капиллярном клубочке"])
            q4 = st.radio("4. В состав первичной мочи в норме НЕ входит:", ["Глюкоза", "Белок", "Вода", "Витамины"])
            q5 = st.radio("5. Обратное всасывание (реабсорбция) происходит в:", ["Канальцах нефрона", "Капсуле", "Почечной лоханке"])
            q6 = st.radio("6. Количество вторичной мочи у взрослого в сутки:", ["180 л", "150 л", "1,5 л", "10 л"])
            q7 = st.radio("7. Гормон, регулирующий количество мочи:", ["Вазопрессин", "Инсулин", "Адреналин"])
            q8 = st.radio("8. Центр мочеиспускания находится в:", ["Головном мозге", "Спинном мозге", "Мозжечке"])
            q9 = st.radio("9. Воспаление почечных лоханок:", ["Цистит", "Пиелонефрит", "Энурез"])
            q10 = st.radio("10. Конечный продукт обмена белков:", ["Мочевина", "Углекислый газ", "Гликоген"])
            ans = f"Био: 1-{q1}, 2-{q2}, 3-{q3}, 4-{q4}, 5-{q5}, 6-{q6}, 7-{q7}, 8-{q8}, 9-{q9}, 10-{q10}"

        elif subject == "Математическая грамотность":
            q1 = st.radio("1. Время выезда туристов из Алматы (чтобы быть в 10:00):", ["8:15", "8:25", "8:35", "8:40"])
            q2 = st.radio("2. Корень уравнения 2x + 5 = 15:", ["2", "5", "7", "10"])
            q3 = st.radio("3. Периметр квадрата (S=64 см²):", ["16 см", "32 см", "24 см", "64 см"])
            q4 = st.radio("4. Какое из чисел является простым?", ["9", "15", "17", "21"])
            q5 = st.radio("5. Продолжите ряд: 1, 4, 9, 16...", ["20", "25", "30", "36"])
            ans = f"Мат: 1-{q1}, 2-{q2}, 3-{q3}, 4-{q4}, 5-{q5}"

        elif subject == "Химия":
            q1 = st.radio("1. Причина высокой щелочности натронного озера:", ["Гидролиз соли", "Испарение", "Разложение воды"])
            q2 = st.radio("2. Тип связи в молекуле воды:", ["Ионная", "Ковалентная полярная", "Металлическая"])
            q3 = st.radio("3. Реакция нейтрализации:", ["Кислота + Основание", "Металл + Вода", "Соль + Соль"])
            q4 = st.radio("4. Валентность углерода в CO2:", ["II", "IV", "VI"])
            ans = f"Хим: 1-{q1}, 2-{q2}, 3-{q3}, 4-{q4}"

        elif subject == "География":
            q1 = st.radio("1. Переезд из Астаны в Хьюстон на ПМЖ — это:", ["Эмиграция", "Иммиграция", "Урбанизация"])
            q2 = st.radio("2. Фактор размещения алюминиевой промышленности:", ["Сырьевой", "Энергетический", "Водный"])
            q3 = st.radio("3. Самый большой океан:", ["Атлантический", "Тихий", "Индийский"])
            ans = f"Гео: 1-{q1}, 2-{q2}, 3-{q3}"

        elif subject == "English":
            q1 = st.radio("1. She ... English every day.", ["learn", "learns", "learning"])
            q2 = st.radio("2. We ... to the museum yesterday.", ["go", "went", "gone"])
            q3 = st.radio("3. This is ... interesting book.", ["a", "an", "the"])
            ans = f"Eng: 1-{q1}, 2-{q2}, 3-{q3}"

        elif subject == "Грамотность чтения":
            st.info("Текст 'Роза': Сад был запущенный... Роза точно плакала...")
            q1 = st.radio("1. Какая информация ОТСУТСТВУЕТ в тексте?", ["Окраска решетки", "Сорная трава", "Лекарственная крапива"])
            q2 = st.radio("2. Выражение 'роза точно плакала' означает:", ["Росинки на лепестках", "Грусть", "Жара"])
            ans = f"Чтение: 1-{q1}, 2-{q2}"

        if st.form_submit_button("🚀 Отправить ответы"):
            if fio:
                new_row = pd.DataFrame([{"ФИО": fio, "Предмет": subject, "Ответы": ans}])
                if os.path.exists(DATA_FILE):
                    df = pd.read_csv(DATA_FILE)
                    df = pd.concat([df, new_row], ignore_index=True)
                else:
                    df = new_row
                df.to_csv(DATA_FILE, index=False)
                st.success("Все ответы успешно сохранены!")
                st.balloons()
            else:
                st.error("Пожалуйста, введите ваше ФИО!")

# --- 4. РЕЖИМ УЧИТЕЛЯ ---
elif role == "Учитель":
    st.title("🔐 Панель учителя")
    if st.text_input("Введите пароль", type="password") == TEACHER_PASSWORD:
        if os.path.exists(DATA_FILE):
            df_view = pd.read_csv(DATA_FILE)
            st.write("### Результаты учеников:")
            st.dataframe(df_view)
            
            if st.button("🤖 Проверить через ИИ"):
                if model is None:
                    st.error("Ошибка: ИИ временно недоступен. Проверьте API_KEY.")
                else:
                    with st.spinner('ИИ сверяет ответы с ключами МОДО...'):
                        try:
                            prompt = f"Ты эксперт МОДО. Проверь ответы: {df_view.to_string()}. Выдай кратко: ФИО, Оценка, Ошибки."
                            response = model.generate_content(prompt)
                            st.markdown(response.text)
                        except Exception as e:
                            st.error(f"Ошибка при запросе: {e}")
        else:
            st.info("Ответов пока нет.")
